echo -n ayoiub ouaziz

char *str[] = {"echo" , "-n", NULL};
char **args[] = {"ayoub" , "ouaziz", null};

execve( "path", str, args);
whle (cmd)
{
	if(cmd[i - 1] == "echo" && cmd[i] == "-n")
	{
		
	}
}
